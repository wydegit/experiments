import numpy as np
import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation

## visualized traing loss curve(iters), evaluating loss curve(iters), evaluating pix mIoU curve(iters)



# def updata(x, y):
#     xdata.append(x)
#     ydata.append(y)
#     ln.set_data(xdata, ydata)
#     return ln,
# def visualize(loss, iters):
#
#
#
#     ani = FuncAnimation(fig, updata, frames=iters, interval=100, blit=True)
#     plt.show()



